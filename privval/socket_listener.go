package privval

import (
	"log"
	"net"
)

// RunSocketListener runs the TCP socket listener and listens for
// the next call.
func RunSocketListener(logger *log.Logger) {
	// TODO
}

// handleMsg runs the loop for the TCP listener to process incoming
// messages.
func handleMsg(conn net.Conn, logger *log.Logger) {
	// TODO
}
